import SwiftUI
import GModApp

/// Replace the new App Playground's generated ContentView with this file.
/// GModMainView discovers the GarrysPAD_Content_*.zip resource automatically.
struct ContentView: View {
    var body: some View {
        GModMainView()
    }
}
